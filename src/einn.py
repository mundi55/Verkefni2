import pygame
import numpy as np

class einn:

    def __init__(self):
        self.status = h
        self.svaedi = 0
        self.xhnit = 0
        self.yhnit = 0
        self.xhradi = 0
        self.yhradi = 0

    def hnit(self):
        x = np.random.rand(n)
        y = np.random.rand(n)

    def hradi(self):
        vx = speed * np.random.rand(n)
        vy = speed * np.random.rand(n)

    def teikna(self):
        #teikna puntk?

