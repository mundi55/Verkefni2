import pygame
import numpy as np

class einn:

    def __init__(self):
        self.status = h
        self.svaedi = 0
        self.x = np.random.rand(n)
        self.y = np.random.rand(n)
        self.vx = speed * np.random.rand(n)
        self.vy = speed * np.random.rand(n)


    def teikna(self):
        pygame.draw.circle(windowSurface, BLUE, \
                           (int(600 * self.x), int(400 * self.y), 5, 0)


    end


